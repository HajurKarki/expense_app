main(){
  // var fname='hajur';
  // var lname='karki';
  // print(fname+' '+lname);
//   var str='Hajur';
//   print(str.toLowerCase()); ///it will convert the letter to small letter the letter H
//   print(str.toUpperCase()); /// it will convert the letter to capital letter the letter H
// print(str.length); /// it will count the number of letter in the string and give the output 5
// print(str.contains('a')); /// it will check the letter a is present in the string or

var str1='hajur';
var str2='karki';
print(str1.compareTo(str2)); /// it will compare the two string and give the output -1 because the first string is less than the second string in alphabetical order
print(str2.compareTo(str1)); /// it will compare the two string and give the output 1 because the second string is greater than the first string in alphabetical order

}